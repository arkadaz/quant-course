states=['High-rate','Middle','Low-rate'];S=np.array([7.12394,5.06302,3.02261]);K=5.25;pay=np.array([177837,0,0]);colors=[GOOD if x>0 else MUTED for x in pay];ax.bar(states,pay,color=colors);ax.set_ylabel('Payer swaption payoff at exercise (USD)');ax.set_title('Exercise only where swap rate exceeds strike');ax2=ax.twinx();ax2.plot(states,S,'o--',color=ACCENT);ax2.axhline(K,color=WARM,ls=':');ax2.set_ylabel('State swap rate (%)')
