m=np.array([1,5,10,25,60]);signal=np.sqrt(m);noise=np.ones_like(m);ax.plot(m,signal/signal[0],marker='o',color=ACCENT,label='true signal SD / 1-min');ax.plot(m,noise,color=BAD,marker='s',label='fixed bounce');ax.set_xscale('log');ax.set_xlabel('aggregation window (minutes, log scale)');ax.set_ylabel('relative size (unitless)');ax.set_title('Aggregation separates persistent signal from fixed bounce',loc='left');ax.legend(fontsize=7);ax.grid(alpha=.2)
