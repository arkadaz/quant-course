slice=np.arange(1,5);fill=np.array([100.04,100.08,100.12,100.18]);mid=np.full(4,100.02);ax.plot(slice,mid,'--',color=MUTED,label='Arrival midpoint');ax.plot(slice,fill,'o-',color=BAD,label='Average fill');ax.set_xlabel('Execution slice');ax.set_ylabel('Price (USD/share)');ax.set_title('Execution price can drift through the order',loc='left');ax.legend(fontsize=7);ax.grid(alpha=.25)
