h=np.arange(0,81);w=.98**h;ax.plot(h,w,color=ACCENT,lw=2,label='GARCH persistence weights');ax.axvline(20,color=BAD,ls='--',label='20-day window edge');ax.step(np.arange(0,41),np.ones(41),where='post',color=WARM,alpha=.65,label='rolling-window memory');ax.set_xlabel('days after shock');ax.set_ylabel('relative weight (unitless)');ax.set_title('GARCH forgets smoothly; a rolling window forgets abruptly',loc='left');ax.legend(fontsize=7)
