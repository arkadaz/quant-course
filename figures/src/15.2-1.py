b=np.linspace(0,1.8,100);req=(.003+.008*b)*100;ax.plot(b,req,color=ACCENT,lw=2,label='SML');names=['JPM','AAPL','NVDA'];bs=[.9,1.1,1.4];ax.scatter(bs,(.003+.008*np.array(bs))*100,color=[GOOD,WARM,BAD],s=42);[ax.annotate(n,(u,v+0.08),fontsize=8) for n,u,v in zip(names,bs,(.003+.008*np.array(bs))*100)];ax.set_xlabel('beta (unitless)');ax.set_ylabel('required return (% per month)');ax.legend(fontsize=8);ax.set_title('Higher beta raises the CAPM hurdle',loc='left')
