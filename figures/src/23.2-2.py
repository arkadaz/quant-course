labels=['รายรับขาย','ซื้อ gas','inject','withdraw','finance','สุทธิ'];vals=[41160,-28000,-1000,-490,-700,10970];x=np.arange(len(vals));colors=[GOOD,BAD,BAD,BAD,BAD,ACCENT];ax.bar(x,vals,color=colors,width=.62);ax.axhline(0,color=INK,lw=.8);ax.set_xticks(x,labels,rotation=25);ax.set_ylabel('USD');ax.set_title('Storage cash-flow bridge',loc='left');ax.grid(axis='y',alpha=.2);[ax.text(i,v+(350 if v>=0 else -750),f'${v:,.0f}',ha='center',fontsize=7.5) for i,v in enumerate(vals)]
