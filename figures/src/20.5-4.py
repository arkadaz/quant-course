ks=np.linspace(.25,1.1,70);ths=np.linspace(.035,.055,70);K,TH=np.meshgrid(ks,ths);J=((K-.65)/.35 + (TH-.045)/.008)**2+0.12*((K-.65)/.35)**2;im=ax.contourf(K,TH*100,np.log10(J+1e-3),levels=18,cmap='viridis');ax.contour(K,TH*100,np.log10(J+1e-3),levels=8,colors='white',linewidths=.4,alpha=.7);plt.colorbar(im,ax=ax,label='Log objective');ax.set_xlabel('Mean-reversion speed');ax.set_ylabel('Long-run level (%)');ax.set_title('Correlated parameters create an objective valley')
