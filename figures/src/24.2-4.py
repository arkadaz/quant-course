labels=['Base','Adverse','Crisis']; losses=np.array([.8,3.5,12.0]); probs=np.array([.70,.25,.05]); weighted=losses*probs; x=np.arange(3); ax.bar(x,losses,color=[GOOD,WARM,BAD]); [ax.text(i,v+.3,f'{v:.1f}M',ha='center',fontsize=8) for i,v in enumerate(losses)]; ax.set_xticks(x,labels); ax.set_ylabel('Scenario loss (USD millions)'); ax.set_title('Scenario loss versus probability',loc='left'); ax.text(1.9,10.2,f'Expected USD {weighted.sum():.2f}M',ha='right',fontsize=8,color=INK); ax.grid(axis='y',alpha=.25)
