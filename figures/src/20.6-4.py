lam=np.linspace(0,.06,100);spread=lam*.6*1e4;pd5=(1-np.exp(-lam*5))*100;ax.plot(lam*100,spread,label='Approximate spread (bp)',color=ACCENT);ax2=ax.twinx();ax2.plot(lam*100,pd5,label='5y default probability',color=BAD);ax.set_xlabel('Hazard rate (% per year)');ax.set_ylabel('Credit spread (basis points)',color=ACCENT);ax2.set_ylabel('Five-year probability (%)',color=BAD);ax.set_title('Annual compensation and horizon probability are different')
