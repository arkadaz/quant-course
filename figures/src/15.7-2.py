A=np.linspace(1,1500,240);gross=np.full_like(A,.005);cost=.0005+.0004*np.sqrt(A/10);net=gross-cost;ax.plot(A,gross*100,color=GOOD,label='gross alpha');ax.plot(A,cost*100,color=BAD,label='cost + impact');ax.plot(A,net*100,color=ACCENT,label='net alpha');ax.axhline(.2,color=MUTED,ls='--',label='required buffer');ax.axvline(390.625,color=INK,ls=':');ax.set_xlabel('AUM (USD million)');ax.set_ylabel('% per month');ax.legend(fontsize=7);ax.set_title('Capacity should stop at the risk buffer, not at zero',loc='left')
