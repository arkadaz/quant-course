mu=np.array([0.0004,0.0003]);S=np.array([[0.0004,0.00024],[0.00024,0.0009]])
x=np.linspace(-0.12,0.12,501);y=np.linspace(-0.18,0.18,501);X,Y=np.meshgrid(x,y)
Z=stats.multivariate_normal(mean=mu,cov=S).pdf(np.dstack((X,Y)))
area=np.trapezoid(np.trapezoid(Z,x,axis=1),y)
ax.bar(['finite-grid integral','theoretical total'],[area,1.0],color=[ACCENT,GOOD])
ax.axhline(1,color=MUTED,ls='--',lw=0.8);ax.set_ylim(0,1.08);ax.set_ylabel('integrated probability')
for i,v in enumerate([area,1.0]):ax.text(i,v+0.02,f'{v:.6f}',ha='center',fontsize=8)
ax.set_title('The stated bivariate PDF integrates to one',fontsize=9,loc='left')
