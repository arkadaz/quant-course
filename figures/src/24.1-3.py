x=np.linspace(-1,1,401); eta=.8; lam=2.25; y=np.where(x>=0,np.maximum(x,0)**eta,-lam*(np.maximum(-x,0)**eta)); ax.plot(x,y,color=ACCENT,lw=2); ax.axvline(0,color=INK,lw=.8); ax.axhline(0,color=INK,lw=.8); ax.set_xlabel('Outcome relative to reference (USD/share)'); ax.set_ylabel('Subjective value (scaled)'); ax.set_title('Illustrative prospect value',loc='left'); ax.grid(alpha=.25)
