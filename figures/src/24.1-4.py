labels=['Gross alpha','Spread','Fee','Impact','Net alpha']; vals=np.array([34200,-2000,-500,-7000,24700]); x=np.arange(len(labels)); colors=[ACCENT,BAD,BAD,BAD,GOOD]; ax.bar(x,vals,color=colors); ax.axhline(0,color=INK,lw=.8); [ax.text(i,v+(900 if v>=0 else -1400),f'${v:,.0f}',ha='center',fontsize=7) for i,v in enumerate(vals)]; ax.set_xticks(x,labels,rotation=20,ha='right'); ax.set_ylabel('USD per signal'); ax.set_title('Illustrative net-alpha accounting',loc='left'); ax.grid(axis='y',alpha=.25)
